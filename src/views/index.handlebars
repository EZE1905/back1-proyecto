<div>
    <h1>Hola {{firstName}}, {{lastName}} bienvenido al sistema!</h1>

    <ul>
        <li>{{age}}</li>
        <li>{{email}}</li>
        <li>{{phone}}</li>
    </ul>

    {{! También podemos utilizar condiciones y loops, pero NO podemos evaluar
    expresiones en la condición, solo pasar variables booleanas ya evaluadas }}
    {{#if isAdmin}}
        <ul>
            {{#each users}}
                <li>{{this.id}}, {{this.firstName}}, {{this.lastName}}</li>
            {{/each}}
        </ul>
    {{/if}}
</div>